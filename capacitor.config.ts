import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'project-ricky-morty',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
